version = "1.0.0"
description = "Shows a custom banner (image or GIF) on your own profile. Client-side only."

aliucord {
    // Plugin changelog shown in Aliucord's plugin list
    changelog = """
        1.0.0
        - Initial release
        - Supports image and GIF banners
        - Settings page to change URL without rebuilding
    """.trimIndent()
}
